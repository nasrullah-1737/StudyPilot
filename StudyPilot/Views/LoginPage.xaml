<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="StudyPilot.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewModels="clr-namespace:StudyPilot.ViewModels"
    x:DataType="viewModels:LoginViewModel"
    Shell.NavBarIsVisible="False"
    BackgroundColor="{StaticResource Background}">
    <ScrollView>
        <VerticalStackLayout Spacing="0">
            <!-- Top gradient hero -->
            <Border Padding="24,56,24,40" StrokeThickness="0">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="{StaticResource GradientStart}" Offset="0" />
                        <GradientStop Color="{StaticResource GradientEnd}" Offset="0.7" />
                        <GradientStop Color="{StaticResource GradientAccent}" Offset="1" />
                    </LinearGradientBrush>
                </Border.Background>
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="0,0,32,32" />
                </Border.StrokeShape>
                <VerticalStackLayout Spacing="12" HorizontalOptions="Center">
                    <Image Source="logo_studypilot.png" HeightRequest="88" WidthRequest="88" />
                    <Label Text="StudyPilot" Style="{StaticResource HeroTitleLabel}" HorizontalOptions="Center" FontSize="32" />
                    <Label Text="Your all-in-one student companion" Style="{StaticResource HeroSubtitleLabel}" HorizontalOptions="Center" />
                </VerticalStackLayout>
            </Border>

            <VerticalStackLayout Padding="24,28,24,32" Spacing="20">
                <Border Style="{StaticResource AppCardStyle}" Margin="0">
                    <VerticalStackLayout Spacing="4">
                        <Label Text="Welcome back" Style="{StaticResource PageTitleLabel}" FontSize="22" />
                        <Label Text="Sign in to continue your study journey" Style="{StaticResource BodyLabel}" Margin="0,0,0,12" />

                        <Label Text="Email address" Style="{StaticResource FormFieldLabel}" />
                        <Border Padding="12,4" BackgroundColor="{StaticResource SurfaceMuted}" Stroke="{StaticResource Border}" StrokeThickness="1">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="12" />
                            </Border.StrokeShape>
                            <Entry Text="{Binding Email}" Keyboard="Email" Placeholder="you@university.edu" BackgroundColor="Transparent" />
                        </Border>

                        <Label Text="Password" Style="{StaticResource FormFieldLabel}" />
                        <Border Padding="12,4" BackgroundColor="{StaticResource SurfaceMuted}" Stroke="{StaticResource Border}" StrokeThickness="1">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="12" />
                            </Border.StrokeShape>
                            <Entry Text="{Binding Password}" IsPassword="True" Placeholder="Enter your password" BackgroundColor="Transparent" />
                        </Border>

                        <Button Text="Sign In" Command="{Binding LoginCommand}" Margin="0,16,0,8" />
                        <Button Text="Create an account" Command="{Binding OpenRegisterCommand}" Style="{StaticResource SecondaryButtonStyle}" />
                    </VerticalStackLayout>
                </Border>
            </VerticalStackLayout>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
